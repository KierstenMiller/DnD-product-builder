describe('template spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
  // it('renders successfully', () => {
  //   cy.visit('http://localhost:3000/build-your-own')
  //   cy.get('h1').should('contain', 'Build Your Own')
  // })
})
