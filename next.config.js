/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true // NOTE: if TRUE strictMode runs every component file twice (for development only - makes sure code is robust) https://stackoverflow.com/questions/71847778/why-is-my-nextjs-component-rendering-twice
}

module.exports = nextConfig
